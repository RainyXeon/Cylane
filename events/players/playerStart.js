module.exports = async (client, player, track) => {
	/**
	 * 
	 * @param {Client} client 
	 * @param {*} player 
	 * @param {*} track 
	 */
	client.logger.info(`Player Started in @ ${player.guildId}`);
};
